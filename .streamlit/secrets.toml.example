# Copy to .streamlit/secrets.toml locally (gitignored — never commit the real key).
# On Streamlit Community Cloud, set this in the app's Settings -> Secrets instead.
GROQ_API_KEY = "gsk_your_key_here"
